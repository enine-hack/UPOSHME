<div class="brand-add">
  <div id="brand-add-title">
    <h1>Ajouter une nouvelle marque</h1>
  </div>
  <form action="/brand-add" method="POST">
  <label>
    <select name="brandname" multiple>
      {{#each brands}}
        <option value="{{id}}">{{brandname}}</option>
      {{/each}}
    </select>
    </label>
  <button>ADD</button>
</form>
<button>RETOUR VERS MES MARQUES</button>
</div>